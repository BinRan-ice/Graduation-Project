var url = 'http://localhost:7000/api/'

// 新增确诊
let preferurl = `${url}newly/diagnosis/`

// 新增治愈
let cureurl = `${url}newly/cure/`

// 新增死亡
let deathurl = `${url}newly/death/`

// 行程数据
let tripurl = `${url}newly/triping/`

// 富文本
let articleurl = `${url}newly/article/`

// 富文本上传图片地址
let articleimgurl = `${url}newly/articleimg/`

// 登录
let loginurl = `${url}newly/login/`


export{preferurl,cureurl,deathurl,tripurl,articleurl,articleimgurl,loginurl}
